@{
    ViewData["Title"] = "Hackathon";
}

@model IEnumerable<Hackathon.Models.Student>
<h2>Edit Students</h2>
    <ul>
    @foreach (var s in Model) {
        <li><a asp-controller="Admin" asp-action="EditStudent" asp-route-id="@s.UserId">@s.FirstName @s.LastName</a></li>
    }
    </ul>
</div>